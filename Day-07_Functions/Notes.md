# Notes for Day 7: Functions

Functions help organize code into meaningful pieces. A program with many repeated blocks becomes easier to understand when common logic is moved into a function. In interviews, function-based problems often examine whether a candidate can break a larger problem into smaller, testable steps.
